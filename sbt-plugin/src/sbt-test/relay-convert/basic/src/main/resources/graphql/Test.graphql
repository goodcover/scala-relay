# Extracted from src/main/scala/example/Test.scala
# This is rad!
query TestQuery {
  defaultSettings {
    notificationSounds
    cache_id
  }
}

mutation TestMutation($input: ActorSubscribeInput!) {
  actorSubscribe(input: $input) {
    clientMutationId
  }
}

fragment Test_fragment on Task {
  title
}

fragment Test_fragmentInterface on Node {
  __typename
  ... on Page {
    name
  }
}

# TODO: Subscription
# TODO: Inputs
