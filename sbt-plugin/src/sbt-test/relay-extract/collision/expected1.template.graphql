# Extracted from ${PWD}/src/main/scala/example2/Test.scala
query TestQuery2 {
  defaultSettings {
    notificationSounds
    cache_id
  }
}

# Extracted from ${PWD}/src/main/scala/example/Test.scala
query TestQuery {
  defaultSettings {
    notificationSounds
    cache_id
  }
}

